export interface UserLogin {
    email: string;
    password: string;
}

export interface UserRegister {
    name: string;
    lastName: string;
    email: string;
    cellPhone: number;
    password: string;
}